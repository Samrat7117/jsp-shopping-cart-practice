<project xmlns="http://maven.apache.org/POM/4.0.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

<modelVersion>4.0.0</modelVersion>

<groupId>com.example</groupId>  <!-- basic packages information -->
<artifactId>jsp-shopping-cart-practice</artifactId>  <!-- basic packages information -->
<version>1.0-SNAPSHOT</version>  <!-- basic packages information -->
<packaging>war</packaging>  <!-- basic packages information -->


<name>jsp-shopping-cart-practice</name> <!-- project name -->

<properties>
    <maven.compiler.source>17</maven.compiler.source> <!-- java source version 17 -->
    <maven.compiler.target>17</maven.compiler.target> <!-- java target version 17 -->
</properties>

<dependencies>

 
<dependency>
    <groupId>jakarta.servlet</groupId> <!--  it us used for htttp servlet, htttp servlet request, http session -->
    <artifactId>jakarta.servlet-api</artifactId> <!--  it us used for htttp servlet, htttp servlet request, http session -->
    <version>6.0.0</version> <!--  it us used for htttp servlet, htttp servlet request, http session -->
    <scope>provided</scope> <!--  it us used for htttp servlet, htttp servlet request, http session -->
</dependency>

<dependency>
    <groupId>jakarta.servlet.jsp</groupId> <!--  that dependecy used for jsp pages -->
    <artifactId>jakarta.servlet.jsp-api</artifactId>  <!--  that dependecy used for jsp pages -->
    <version>3.1.0</version> <!--  that dependecy used for jsp pages -->
    <scope>provided</scope> <!--  that dependecy used for jsp pages -->
</dependency> 


<dependency>
    <groupId>org.glassfish.web</groupId> <!-- that dependecy used for c:foreach,c:choose,c:when -->
    <artifactId>jakarta.servlet.jsp.jstl</artifactId> <!--  that dependecy used for c:foreach,c:choose,c:when -->
    <version>2.0.0</version> <!--  that dependecy used for c:foreach,c:choose,c:when -->
</dependency>



</dependencies>

<build>
    <finalName>jsp-shopping-cart-practice</finalName>
</build>


</project>

<!-- 
// project object model - pom

// pom xml is used to manage dependencies

// confugure project

// build and manage the application -->


<!-- user submit a form post/cart -->
<!-- web.xml - map the cartservlet -->
<!-- redirect to the /cart -->
<!-- doget method run or forword to the cart.jsp -->